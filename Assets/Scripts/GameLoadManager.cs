using UnityEngine;

// Manages the game loading flag to determine if a saved game should be loaded.
public static class GameLoadManager
{
    public static bool ShouldLoadGame = false; // Indicates whether the game should load a saved state
}